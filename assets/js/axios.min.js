function loadProducts() {
    axios.get(apiUrl)
        .then(response => {
            appState.products = response.data;
            displayProducts();
        })
        .catch(error => {
            console.error('Error al obtener productos:', error);
        });
}
